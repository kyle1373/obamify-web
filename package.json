{
  "name": "obamify-web",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "wasm:build": "trunk build --release --dist public/wasm --public-url /wasm/",
    "wasm:ensure": "node scripts/ensure-wasm.mjs",
    "sync:assets": "node scripts/sync-assets.mjs",
    "predev": "npm run sync:assets && npm run wasm:ensure",
    "prebuild": "npm run sync:assets && npm run wasm:build"
  },
  "dependencies": {
    "next": "^14.2.11",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.16.6",
    "@types/react": "^18.3.9",
    "@types/react-dom": "^18.3.0",
    "eslint": "^8.57.1",
    "eslint-config-next": "^14.2.11",
    "typescript": "^5.6.3"
  }
}
